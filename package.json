{
  "name": "outran-commerce-system",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "backend:dev": "uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "frontend"
  ]
}
